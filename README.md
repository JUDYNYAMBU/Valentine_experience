
  # Interactive Valentine Experience

  This is a code bundle for Interactive Valentine Experience.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
